import { TextSpellDirective } from './text-spell.directive';

describe('TextSpellDirective', () => {
  it('should create an instance', () => {
    const directive = new TextSpellDirective();
    expect(directive).toBeTruthy();
  });
});
